[app]

title = Multiverse Cards

package.name = mcards
package.domain = com.megagamer69

source.dir = ./source
source.exclude_dirs = tests, bin, venv
source.exclude_patterns = README

version.regex = "1.0.0"

requirements = python3, pygame-ce, cython

orientation = portrait
